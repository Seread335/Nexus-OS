# PKGBUILD skeleton for hashcat (AUR)

pkgname=hashcat
pkgver=6.2.6
pkgrel=1
pkgdesc="Advanced GPU-based password recovery"
arch=('x86_64')
url="https://hashcat.net/hashcat/"
license=('MIT')
depends=('ocl-icd' 'libpcap')
makedepends=('gcc' 'make' 'cmake')
source=("https://hashcat.net/files/hashcat-${pkgver}.7z")
sha256sums=('SKIP')

prepare() {
  # extraction steps
  echo "Prepare hashcat source"
}

build() {
  echo "hashcat build - usually precompiled binaries; implement as needed"
}

package() {
  echo "Install files into package dir"
}
